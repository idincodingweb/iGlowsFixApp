import 'package:flutter/material.dart';
import '../../salon/salon_screen.dart';

class SalonTab extends StatelessWidget {
  const SalonTab({super.key});

  @override
  Widget build(BuildContext context) => const SalonScreen();
}
