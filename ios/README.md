# iOS scaffold

Folder ini placeholder untuk struktur iOS. Jika ingin build iOS penuh, jalankan:

```bash
flutter create . --project-name iglows --org com.iglows --platforms=ios
```

Lalu jalankan `flutterfire configure` untuk membuat `GoogleService-Info.plist`.